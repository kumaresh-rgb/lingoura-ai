namespace Lingoura.Api.IntegrationTests.Authentication;

// Integration tests require a running PostgreSQL instance.
// Use Testcontainers to spin one up automatically:
//   docker must be running for these tests to execute.
public sealed class AuthEndpointTests
{
    // TODO: Add WebApplicationFactory + Testcontainers setup.
    // Placeholder so the project compiles clean.
    [Fact]
    public void Placeholder_test()
    {
        Assert.True(true);
    }
}
